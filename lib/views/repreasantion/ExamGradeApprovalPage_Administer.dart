import 'package:flutter/material.dart';

class ExamGradeApprovalPage extends StatelessWidget {
  const ExamGradeApprovalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('صفحة قبول درجات الامتحانات'),
    );
  }
}
