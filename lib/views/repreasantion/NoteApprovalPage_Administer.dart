import 'package:flutter/material.dart';

class NoteApprovalPage extends StatelessWidget {
  const NoteApprovalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('صفحة قبول الملاحظات'),
    );
  }
}
